import React,{ Commponent } from 'react';

const mapStyles = {
  map: {
    position: 'absolute',
    width: '100%',
    height: '100%'
  }
};

export class OrderCurrentLocation extends Component {
  // ...
}

export default OrderCurrentLocation;
